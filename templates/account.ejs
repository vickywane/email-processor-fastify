<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>

    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <br />
    <br />
    <div class="text-center">
      <h1 class="text-2xl">USER ACCOUNT PAGE</h1>
      <br />
      <hr />
      <br />
    </div>

    <button id="start-email-integration">Start Email Integration</button>
  </body>

  <script>
    const button = document.getElementById("start-email-integration");
    button.addEventListener("click", async () => {
      const response = await fetch("/create-integration", {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify({}),
      });
    });
  </script>
</html>
